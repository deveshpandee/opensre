id: 206-servicenow-configured-check-verify
title: ServiceNow configured check runs verifier
intent_class: local_execution
input:
  prompt: Is ServiceNow configured?
session:
  has_prior_state: false
  configured_integrations: []
  resolved_integrations: {}
available_capabilities:
  slash_commands:
  - /integrations
  cli_commands: []
  synthetic_suites: []
notes:
- "Regression for #3102: ServiceNow configuration questions should execute the verifier, not suggest a CLI command."
turn:
  expected_kind: agent
policy:
  executes_terminal_action: true
planned_actions:
- kind: slash
  content: /integrations verify servicenow
  source: llm
  target_surface: slash
  command: /integrations
  args:
  - verify
  - servicenow
response_contract:
  must_contain_any:
  - ran /integrations verify servicenow
  must_not_contain: []
history:
  expected:
  - type: slash
    ok: true
runs: 1
tool_actions:
- surface: dispatch
  kind: slash
  command: /integrations
  args:
  - verify
  - servicenow
  content: /integrations verify servicenow
